// this handler checks fle mime type and add furk uploader variant
/*window.addEventListener('load', function(e) {
    // get original browser window
    var wm = Components.classes["@mozilla.org/appshell/window-mediator;1"]
           .getService(Components.interfaces.nsIWindowMediator);
    var browserWindow = wm.getMostRecentWindow("navigator:browser");

    //browserWindow.Firebug.Console.log(document.getElementsByTagName('image')[0]);
    // TODO info isn't full. screenshorts isn't exists in this info file

    if(gAlertListener && gAlertTextClickable && browserWindow.furkUploader.info[gAlertCookie]) {

        var info = browserWindow.furkUploader.info[gAlertCookie];

        for(var i=0; i<3; i++) {
            document.getElementById('ss' + i).src = info.ss_urls_tn[i];
        }

        document.getElementById('ssbox').parentNode.parentNode.collapsed = false;

    }

}, true);*/
